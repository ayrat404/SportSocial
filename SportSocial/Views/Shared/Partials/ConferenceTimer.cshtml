@model BLL.Admin.Conference.ViewModels.ConfModel
@using DAL.DomainModel.EnumProperties;

@if (Model != null)
{
    if (Model.Status == ConfStatus.Process)
    {
        <div class="block">
            <div class="conf-date">
                <div>
                    <a href="@Model.Url" class="conf-date__link">Видеоконференция тему "@Model.Title" уже началась! Ссылка</a>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="block">
            <div conf-date="@Model.Stamp"></div>
        </div>
    }
}

@*Доступпные поля*@
@*@Model.Title;
@CurrentUser.User.Profile.Avatar;
@Model.Description;
@Model.Date;
@Model.Status;
@Model.Stamp;
@(Model.Status == ConfStatus.Process)*@