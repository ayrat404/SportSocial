@model BLL.Admin.Conference.ViewModels.ConfModel
@using DAL.DomainModel.EnumProperties;

@if (Model != null)
{
    <div class="block">
        <div conf-date="@Model.Stamp"></div>
    </div>
}


@*Доступпные поля*@
@*@Model.Title;
@CurrentUser.User.Profile.Avatar;
@Model.Description;
@Model.Date;
@Model.Status;
@Model.Stamp;
@(Model.Status == ConfStatus.Process)*@