'use strict';

// смена картинок на фоне
// ---------------
angular.module('shared').directive('bgSlider', ['$timeout', function ($timeout) {
    return {
        restrict: 'A',
        link: function($scope, element, attrs) {
            
        }
    }
}]);