'use strict';

angular.module('socialApp.controllers')
    .controller('SupportModalCtrl', [
        '$scope',
        'mixpanel',
        function (
            $scope,
            mixpanel) {

            $scope.submit = function() {
                
            }

        }
    ]);