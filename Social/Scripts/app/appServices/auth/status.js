'use strict';

// user status service (user, admin, etc)
angular.module('appSrvc')
    .factory('statusSrvc', [
        function () {

            // ---------------
            return {}
        }
    ]);
