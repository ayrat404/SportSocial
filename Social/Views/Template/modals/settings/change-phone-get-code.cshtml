<div class="fs-modal__title">@R("Изменение номера мобильного телефона")</div>
<div class="fs-modal__body">
    <p class="text-center">@R("Ваш номер телфона не будет виден для пользователей. Предназначен только для входа в личный кабинет").</p>
</div>
<div class="fs-modal__body">
    <div data-server-validation="serverValidation">
        <form name="GetCodeForm"
              ng-submit="submit()">
            <div class="fs-form__row">
                <input name="phone" type="text" ng-model="phone" required placeholder="@R("Номер телефона без +7")" class="fs-input" />
            </div>
            <div class="fs-modal__submit">
                <button ng-disabled="!GetCodeForm.$valid" class="btn btn-fill-green btn-xl" type="submit">@R("Получить код доступа")</button>
            </div>
        </form>
    </div>
</div>