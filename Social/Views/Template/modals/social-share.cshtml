<div class="fs-modal__title">@R("Поделиться записью")</div>
<div class="fs-modal__submit">
    <social-share ss-url="{{o.url}}"
                  ss-title="{{o.title}}"
                  ss-text="{{o.text}}
                  ss-tags={{o.tags}}"></social-share>
</div>